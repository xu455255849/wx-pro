

import request from 'request-promise'

class Wechat {
  constructor (opts) {

  }

  async request (options) {
    const res = await this.request(options)

    return res
  }

  async fetchAccessToken () {
    if (isValid(data)) {
      return await this.upda
    }
  }

  async updetaAccessToken () {

  }
}